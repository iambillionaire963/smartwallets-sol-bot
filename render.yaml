services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: cd telegram-premium-bot && pip install -r requirements.txt
    startCommand: cd telegram-premium-bot && python bot.py
    plan: free
